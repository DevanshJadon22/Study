//using get element by id method of document object to access the html element(tag) using its allocated id.
let heading=document.getElementById("heading");
//here the document.getelementbyid("heading"); will return the value of html element having id="heading" that is this will return h1 to us and here we are storing it in heading variable.
console.dir(heading);