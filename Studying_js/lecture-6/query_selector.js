let firstEl=document.querySelector('p');// this line of code returns us the first element whose tag is "p" or paragraph tag.
console.dir(firstEl);
let allEl=document.querySelectorAll('p');// this line of code returns us the all element whose tag is "p" or paragraph tag.
console.dir(allEl);