function sum(x,y){
    // here the parameters of this functions are like local variable of function and has block scope
    let s=x+y;
    return s;
}
//here return statement can return one variable(value it may be a variable ,array or anything)
ans=sum(1,3);
console.log(ans);